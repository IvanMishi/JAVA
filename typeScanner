public static void main (String[] args) {
String s1 = "values";//Cоздать переменную класса String ссылающаяся на значение "values"
String s2 = new String ("valuesObj");//new - ключевое слово которое обозначает создание нового объекта класса, в параметрах указываем конкретные значения - это объект

//Класс Scanner отвечает за ввод данных от пользователя
//scn - ссылается на объект класса сканер
//класс Scanner требует импорта java.util после чего мы можем работать с классом Scanner и получать данные на вход
//в параметры необходимо задать входной поток  System.in - стандартный поток входных данных
Scanner scn = new Scanner (System.in);
System.out.println("Введите какие-нибудь данные");

//Создаем объект класса String и ссылаемся на то, что будет получено с клавиатуры
//Метод nextLine - считать с клавиатуры одну строчку чего-угодно и строчка продолжается до тех пор пока мы не нашем ENTER (Переход на следующую строчку)
//Переменная scnString будет хранить то, что мы ввели с клавиатуры
String scnString = scn.nextLine();
System.out.println("Вы ввели строку - " + scnString);
//Ввести числовые данные по аналогии с методом nextLine
int scnInt = scn.nextInt();
System.out.println("Вы ввели чило " + scnInt);
}
