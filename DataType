public class DataType {
    public static void main(String[] args) {
        //Переменные и примитивные типы данных.
        //Переменная - это ячейка которая хранит каки-либо значения чтоб ее использовать нужно сначала ее задекларировать, затем инициализировать 
        int name; //Рекомендуется размещать одну декларацию на строку, для коментирования, не стоит делать так - (int value1, value2;) 
        //int тип переменной - множество всех целых чисел 32 бит
        //Декларация(выделить память) - int (тип переменной) = name (имя переменной с маленькой буквы)
        name = 12345; //Инициализация - "имя переменной" = "значения" (соответствующие ее типу дынных, целые числа int)
        System.out.println(name);//Вывести в терминал переменную через параметр
        
        int secondName = 54321;
        System.out.println(secondName);

        //Строки(String) в Java. Ссылочные типы данных.
        String s = "value"; // Переменная s ссылается на объект класса String  = "value" - это объект созданый с помощью класса String
        //Тип данных строка -  "имя" = "значение";
        //String - это класс , а не примитив
        //Класс не имеет ничего общего с объектом, Класс - схема создания объекта
        //Объект - на основе этой схемы
        
        //Пример
        String a = "a";//Переменная a класса String ссылается на объект => "a"
        String b = "b";//Переменная b класса String ссылается на объект => "b"
        int i = 25;// i примитив типа int  хранит в себе = "25"
        System.out.print(a + "+" + b + "=" + i + s + ":");// Объеденение переменных и вывод в виде строк
  }
}
